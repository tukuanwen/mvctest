<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tu.mvctest.pojo.HelloMapping">
    <cache />

    <select id="findOne" resultType="com.tu.mvctest.pojo.bean.HelloBean">
        SELECT * FROM hello he WHERE he.sname = #{sname}
    </select>

</mapper>